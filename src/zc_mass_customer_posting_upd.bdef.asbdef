projection;
strict ;
use draft;

define behavior for ZC_Mass_customer_posting_upd //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action ProcessFile;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;
}